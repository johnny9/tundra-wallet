<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- No INTERNET or CAMERA permission in this offline milestone. No broad file access. -->
    <application android:label="Tundra Dev" android:allowBackup="false"
        android:usesCleartextTraffic="false" android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>

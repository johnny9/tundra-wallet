name: Collect Android dependency review
on:
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: android-dependency-review-${{ github.ref }}
  cancel-in-progress: false
jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3 # v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407 # v3
      - run: sdkmanager 'platforms;android-36' 'build-tools;36.0.0'
      - uses: gradle/actions/setup-gradle@ed408507eac070d1f99cc633dbcf757c94c7933a # v4
      - name: Collect candidate locks and checksums for review
        run: >-
          ./apps/android/gradlew --no-daemon -p apps/android
          -I "$GITHUB_WORKSPACE/scripts/gradle-dependency-review.init.gradle"
          --write-locks --write-verification-metadata sha256
          resolveDependencyReview :app:resolveDependencyReview
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: android-dependency-review
          if-no-files-found: error
          path: |
            apps/android/gradle.lockfile
            apps/android/buildscript-gradle.lockfile
            apps/android/app/gradle.lockfile
            apps/android/app/buildscript-gradle.lockfile
            apps/android/gradle/verification-metadata.xml

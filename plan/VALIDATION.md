# Validation status

## Sync implementation pass — September 8, 2026

Locally executed: 64 core unit tests, 5 adversarial loopback HTTP tests, 23 offline checks,
formatting and strict Clippy pass. The separate real Bitcoin Core 31.1 regtest scenario
passes: keyless mining, coinbase maturity, persistence, shorter/replacement reorgs,
draft invalidation and durable user freezes. Both language bindings generate from the
new compiled Rust library. Native sync changes await this pass's CI build/runtime tests;
the native build evidence below belongs to the earlier named commit.

The HTTP tests cover endpoint failure/wrong network, redirects, oversized/malformed responses,
cancellation during IO, concurrent address issuance, failed database commit, and retention
of the previous successful timestamp. These are development tests, not independent security
review or hardware qualification. Real QR/USB signing and broadcast remain unavailable.

## Previous compiled foundation

Planning baseline: September 7, 2026. Current validation: **September 8, 2026**.
**M1 is still open. The debug APK is a development artifact; no supported hardware or real-funds readiness is claimed.**

## Executed checks

| Check | Actual result |
|---|---|
| Rust workspace | **60 passed**, 0 failures; all targets and features, including the binding generator, compiled with Rust/Cargo 1.93.1 |
| Formatting | `cargo fmt --all --check` passed after formatting the handoff sources |
| Clippy | `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` passed |
| Offline SQLite / public fixture / source checks | **23 passed**, 0 failures, 0 errors |
| Cargo lockfile | Generated by Cargo; reviewed 122 checksummed crates.io dependencies and two workspace packages; no Git dependency sources |
| UniFFI Kotlin and Swift | Both generated from the same compiled host library; generator uses the crate-local config without deprecated override arguments |
| Gradle wrapper | Generated by Gradle 8.13; distribution and JAR verified against official SHA-256 values; `gradlew --version` passed on JDK 17.0.20 without sudo |
| Android app | Debug APK built in CI; arm64-v8a and x86_64 Rust libraries verified inside the downloaded APK |
| iOS simulator target | Xcode simulator-target build passed in CI with code signing disabled; no simulator/phone runtime test |
| Kotlin / Swift FFI runtime checks | Three Kotlin/JVM tests and three macOS Swift smoke tests passed against the real host library: Unicode/reopen, typed errors and integer amounts |
| QR / USB / bhwi / signing / finalization / broadcast | NOT IMPLEMENTED OR TESTED |
| Git | Implementation commits pushed to `origin/master` at `johnny9/tundra-wallet` |
| Approved design | All four reference SHA-256 checks passed; rebuilding HTML from editable sources matched `design/prototype.html` byte for byte |

Executed `scripts/check.sh` with a writable Cargo cache under ignored `build/cargo-home`.
Host: Ubuntu 26.04 x86_64, Rust/Cargo 1.93.1, rustfmt 1.8.0, Clippy 0.1.93. Initially the
matching Ubuntu formatter/linter binaries were extracted into the ignored build directory;
the owner subsequently installed the system packages and their versions were verified.
The same checks also passed with official Rust 1.93.1 installed through Rustup in the ignored
build directory. `source build/dev-env.sh` selects that local toolchain, Android Rust targets
and Gradle cache on this checkout; clean clones follow the setup instructions in README.
Android SDK/NDK and macOS/Xcode builds were supplied by CI. They are not installed locally.
Node syntax and browser behavior were not retested in this pass (Node is absent here).

The initial offline run failed two publisher tests because the referenced
`scripts/publish-github.sh` does not exist in this checkout. Those obsolete checks were
removed; ordinary Git now targets the owner's configured repository. The hardware source
guard was made whitespace-tolerant after rustfmt. No wallet behavioral test was removed.

Current offline evidence is checked in at `validation/offline-checks.json` and
`validation/offline-checks.log`. Full local Rust output is in ignored `build/check.log` and `build/check-official.log`.
The offline report only describes its own checker; it does not certify Rust/native builds.
Rust source checks cannot establish native packaging, FFI lifetime behavior or device support.

The [successful native CI run](https://github.com/johnny9/tundra-wallet/actions/runs/34248953834)
for commit `6b209836af16beee374b6d2429cf20ff005ef403` passed Rust, Android and iOS jobs.
The checked-in `validation/ci-checks.json` records job URLs, counts and the development APK's
SHA-256. `validation/jvm-tests.xml` is the downloaded JUnit report: three tests, no failures,
errors or skips. The development APK is available as a CI artifact and locally at
`build/final-apk/app-debug.apk`. These are host language roundtrips and app compilation,
not mobile UI/device qualification.

The [first CI run](https://github.com/johnny9/tundra-wallet/actions/runs/34246691081) passed
Rust and the iOS simulator-target build. Android reached Kotlin compilation and exposed the
exception-field naming collision. Generated sources are regenerated from Rust rather than
patched by hand. The Kotlin adapter displays only the bounded `detail` error string.

## Descriptor identity and persistence regressions

Six additional descriptor tests and five file-backed engine tests pass. The reordered-key
and mixed receive/change-order tests failed before the fix: the same sortedmulti script
could acquire a second wallet ID. Import now canonicalizes key order and recognizes older
order-dependent IDs without renaming wallet rows or losing metadata.

The persistence tests reopen actual Rust-created SQLite databases, preserve unknown balances,
receive indices, Unicode labels, unsigned PSBTs, freezes and reservations, inject a failed
snapshot write after draft/reservation creation, and race two independent connections for
one coin. They test transaction rollback and reopen behavior, not power-loss recovery or
mobile process termination. `tempfile` 3.27.0 is now a direct test-only dependency; its
MIT/Apache-2.0 license was checked and it was already present in Cargo.lock.

## Remaining gates

No Android emulator/instrumentation run, iOS simulator runtime session, physical phone or
hardware wallet has been tested. Mobile file protection, import/restart flows, lifecycle,
accessibility and appearance still require acceptance checks. Chain sync, QR/USB transport,
signature acceptance, finalization and broadcast remain unavailable. Mainnet spending stays
gated in Rust. M1 is not complete and no later milestone has been declared complete.

The [original handoff report](https://github.com/johnny9/tundra-wallet/blob/995026c/plan/VALIDATION.md)
and its original 25-check offline evidence are preserved in the initial source commit.
The current offline suite has 23 checks because the removed publishing script is no longer
part of the project. Offline SQL/source checks alone do not prove Rust behavior or security.
